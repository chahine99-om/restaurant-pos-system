# Database (never commit real credentials)
# With Docker: docker-compose up -d then use the URL below
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/restaurant_pos?schema=public"

# JWT (use long random secrets in production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=15m

# App
NODE_ENV=development
PORT=3000

# CORS - comma-separated allowed origins (never use * in production)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Frontend URL for email verification links (dev: http://localhost:5173)
FRONTEND_URL=http://localhost:5173

# Optional: SMTP for sending verification emails. If not set, link is logged and returned in API response (dev).
# MAIL_HOST=localhost
# MAIL_PORT=587
# MAIL_SECURE=false
# MAIL_USER=
# MAIL_PASS=
# MAIL_FROM=noreply@restaurant.local
